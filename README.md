# HyperLiquid Node Parser

FastAPI приложение для парсинга логов HyperLiquid node и предоставления данных order book через REST API.

## 🚀 Возможности

- **Парсинг логов** - автоматическое извлечение ордеров из JSON логов
- **Real-time мониторинг** - отслеживание изменений файлов в реальном времени
- **REST API** - полный набор endpoints для работы с ордерами
- **Конфигурация** - гибкая настройка через API и файлы
- **Логирование** - централизованное логирование с ротацией
- **Тестирование** - полное покрытие тестами (TDD подход)
- **Оптимизация производительности** - поддержка файлов размером до 50 ГБ
- **Docker консистентность** - единый пользователь `hl` для хоста и контейнера

## 🏗️ Архитектура

```
src/
├── main.py              # FastAPI приложение
├── api/
│   └── routes.py        # API endpoints
├── parser/
│   ├── log_parser.py    # Парсер логов (асинхронный)
│   └── order_extractor.py # Извлечение ордеров
├── storage/
│   ├── models.py        # Pydantic модели
│   ├── file_storage.py  # Файловое хранение
│   ├── order_manager.py # Управление ордерами (оптимизированное)
│   └── config_manager.py # Управление конфигурацией
├── watcher/
│   └── file_watcher.py  # Мониторинг файлов (с таймаутами)
└── utils/
    └── logger.py        # Логирование
```

## 📋 Требования

- Python 3.11+
- FastAPI
- Uvicorn
- Pydantic
- Watchdog
- aiofiles

## 🛠️ Установка

1. **Клонировать репозиторий:**
```bash
git clone <repository-url>
cd HyperNodeServer
```

2. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настроить конфигурацию:**
```bash
# Создать .env файл
cp .env.example .env
# Отредактировать настройки
```

4. **Запустить приложение:**
```bash
python run.py
```

## 🐳 Docker

### Подготовка пользователя

**Перед запуском создайте пользователя `hl` на хосте:**
```bash
# Запустить скрипт автоматической настройки
chmod +x scripts/setup-user.sh
./scripts/setup-user.sh
```

**Или создать вручную:**
```bash
# Создать пользователя hl с UID/GID 1000
sudo groupadd -g 1000 hl
sudo useradd -u 1000 -g hl -m -d /home/hl hl
sudo mkdir -p /home/hl/hl/data
sudo chown -R hl:hl /home/hl/hl
```

### Запуск с Docker Compose

**Быстрый деплой с Makefile (рекомендуется):**
```bash
# Полный деплой в одну команду (setup + build + start)
make deploy

# Или поэтапно:
make setup    # Настройка пользователя
make start    # Запуск контейнеров
make logs     # Просмотр логов
make stop     # Остановка
```

**Ручной запуск:**
```bash
# После настройки пользователя
docker-compose up -d

# Остановка
docker-compose down
```

### Ручной запуск Docker

**Сборка образа:**
```bash
docker build -t hyperliquid-parser .
```

**Запуск контейнера:**
```bash
docker run -p 8000:8000 \
  -v ~/hl/data:/app/node_logs \
  -v ./logs:/app/logs \
  --user 1000:1000 \
  hyperliquid-parser
```

> ⚠️ **Важно**: Контейнер запускается от пользователя `hl` (UID:1000) для обеспечения корректных прав доступа к файлам между хостом и контейнером.

## ⚡ Оптимизация производительности

### Проблемы с большими файлами (30+ ГБ)

Приложение оптимизировано для работы с файлами размером до 50 ГБ:

- **Асинхронное чтение** - файлы читаются по частям (chunks) без блокировки
- **Батчевая обработка** - ордера обрабатываются группами по 1000 штук
- **Таймауты** - автоматические таймауты для предотвращения зависания
- **Ограничения размера** - настраиваемые лимиты на размер файлов и количество ордеров

### Настройки производительности

```python
# config/settings.py
MAX_FILE_SIZE_GB: float = 50.0           # Максимальный размер файла
MAX_ORDERS_PER_FILE: int = 1000000       # Максимум ордеров на файл
CHUNK_SIZE_BYTES: int = 8192             # Размер чанка для чтения
BATCH_SIZE: int = 1000                   # Размер батча для обработки
PROCESSING_TIMEOUT_PER_GB: int = 5       # Таймаут в секундах на ГБ
```

### Мониторинг производительности

```bash
# Проверить состояние приложения
curl http://localhost:8000/performance

# Проверить здоровье системы
curl http://localhost:8000/health

# Мониторинг логов в реальном времени
tail -f logs/app_*.log
```

### Рекомендации для продакшена

1. **Мониторинг памяти** - следите за потреблением RAM
2. **Таймауты** - настройте таймауты под ваши требования
3. **Батчи** - увеличьте размер батчей для лучшей производительности
4. **Логирование** - используйте DEBUG уровень только для отладки

## 📖 API Документация

### Основные Endpoints

#### Ордера

- `GET /api/v1/orders` - получить список ордеров с фильтрацией
- `GET /api/v1/orders/{order_id}` - получить ордер по ID
- `GET /api/v1/orders/stats/summary` - статистика ордеров

#### Конфигурация

- `GET /api/v1/config` - получить текущую конфигурацию
- `PUT /api/v1/config` - обновить конфигурацию

#### Система

- `GET /` - информация о приложении
- `GET /health` - проверка состояния
- `GET /performance` - информация о производительности
- `GET /docs` - Swagger документация

### WebSocket Endpoints

#### Real-time уведомления

- `ws://localhost:8000/ws/orderUpdate` - мгновенные уведомления об изменениях ордеров
- `ws://localhost:8000/ws/orderBatch` - батчевые уведомления каждые 500ms
- `GET /ws/status` - статус WebSocket соединений

#### Формат сообщений

**Мгновенные уведомления:**
```json
{
  "type": "orderUpdate",
  "channel": "orderUpdate",
  "timestamp": "2024-01-01T12:00:00",
  "data": {
    "id": "123",
    "symbol": "BTC",
    "side": "buy",
    "price": 50000.0,
    "size": 1.0,
    "status": "open"
  }
}
```

**Батчевые уведомления:**
```json
{
  "type": "orderBatch",
  "channel": "orderBatch",
  "timestamp": "2024-01-01T12:00:00",
  "data": {
    "orders": [...],
    "count": 5
  }
}
```

### Примеры запросов

```bash
# Получить ордера с лимитом
curl "http://localhost:8000/api/v1/orders?limit=100"

# Получить ордера по символу
curl "http://localhost:8000/api/v1/orders?symbol=BTC"

# Проверить производительность
curl "http://localhost:8000/performance"
```

## 🔧 Конфигурация

### Переменные окружения

```bash
# Путь к логам
NODE_LOGS_PATH=/app/node_logs

# Лимиты производительности
MAX_FILE_SIZE_GB=50.0
MAX_ORDERS_PER_FILE=1000000
CHUNK_SIZE_BYTES=8192
BATCH_SIZE=1000

# Таймауты
PROCESSING_TIMEOUT_PER_GB=5

# API настройки
API_HOST=0.0.0.0
API_PORT=8000
```

## 🚨 Устранение проблем

### Приложение зависает при обработке больших файлов

1. **Проверить настройки:**
   ```bash
   curl http://localhost:8000/performance
   ```

2. **Уменьшить размер батчей:**
   ```bash
   # В .env файле
   BATCH_SIZE=500
   ```

3. **Установить таймауты:**
   ```bash
   # В .env файле
   PROCESSING_TIMEOUT_PER_GB=3
   ```

4. **Ограничить размер файлов:**
   ```bash
   # В .env файле
   MAX_FILE_SIZE_GB=30.0
   ```

### HTTP-сервер не отвечает

1. **Проверить логи:**
   ```bash
   tail -f logs/app_*.log
   ```

2. **Проверить состояние файлового монитора:**
   ```bash
   curl http://localhost:8000/performance
   ```

3. **Перезапустить приложение:**
   ```bash
   docker-compose restart
   ```

## 📊 Мониторинг

### Логи производительности

Приложение автоматически логирует:
- Медленные запросы (>1 секунды)
- Таймауты обработки файлов
- Размеры обрабатываемых файлов
- Количество обработанных ордеров

### Метрики

- Время обработки запросов
- Количество ордеров в памяти
- Статус файлового монитора
- Размеры обрабатываемых файлов

## 🔄 Обновление

```bash
# Остановить приложение
docker-compose down

# Обновить код
git pull origin main

# Пересобрать и запустить
docker-compose up --build -d
```

## 📝 Лицензия

MIT License
